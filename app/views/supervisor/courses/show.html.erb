<%= render @course %>
<%= link_to "delete", supervisor_course_path,
  method: :delete, data: {confirm: t(:confirm_notification)} %>
<%= link_to "edit", edit_supervisor_course_path %>

<h3>List subject</h3>
<%= render @course.subjects %>